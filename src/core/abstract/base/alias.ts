export enum ServiceAlias {
  SESSION = 'session',
  ROOM = 'room',
  GAME = 'game',
}

export enum RegistryAlias {
  HUB = 'hub',
}

export enum StoreAlias {
  ROUTER = 'router-store',
  SESSION = 'session-store',
  ROOM = 'room-store',
}
